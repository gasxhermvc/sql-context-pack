# Project memory

- Current normative Requirement is `docs/spec/design-spec-v1.6.md`; product version is `1.2.0`. v1.6 preserves the complete v1.5 text and inserts the owner-approved session-profile/managed-service revision with explicit supersession only where behavior changed. Archived Requirement versions remain immutable.
- Current runtime is the owner-approved automatic loopback-only `SQLContextPack` Windows Service plus a plugin-bundled per-Codex-session MCP bridge. The bridge retains the active profile only in room memory and injects it into explicit-profile catalog requests; profile changes never restart the shared service.
- Task `20260719020226-implement-approved-requirement-v1-6` resolved the Windows service/update decisions: LocalSystem service under protected ProgramData, trusted recorded checkout or explicit local source, transactional `sqlctx update`/`repair`, exact-version authenticated health, rollback, and stale-transaction cleanup.
- Installed evidence on 2026-07-19: service is Automatic/Running, authenticated health is `ok` version `1.2.0`, the personal plugin is installed/enabled and schema-valid, and the full writer gate passes Ruff, mypy, 85 tests, build, and zero repository residue.
- The encrypted `agrimap-dev` host had accidental surrounding single quotes. Removing only those quotes changed the real API result from `DATABASE_HOST_UNREACHABLE` to `DATABASE_TLS_CERTIFICATE_UNTRUSTED`, proving the named SQL Server endpoint was reached and isolating the trust-policy decision later resolved by v1.7.
- Requirement v1.7 records the owner's explicit approval for per-profile development certificate trust on `agrimap-dev`. Installed API profile test now returns HTTP 200/reachable; a real catalog discovered and fully analyzed 778/778 objects with zero failures. A bounded one-object export attempted from the cooperatively cancelled catalog returned sanitized `INTERNAL_ERROR`, so export/validation closure remains open as a separate defect.
